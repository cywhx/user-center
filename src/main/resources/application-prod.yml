spring:
  # DataSource Config
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://bj-cynosdbmysql-grp-a3cnf1gc.sql.tencentcdb.com:26273/user_center
    username: root
    password: 
  # session 失效时间
  session:
    timeout: 86400
